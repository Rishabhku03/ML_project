---
phase: 02-real-time-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/synthetic_traffic_generator.py
  - tests/test_traffic_generator.py
autonomous: true
requirements:
  - INGEST-04

must_haves:
  truths:
    - "Traffic generator sends HTTP POST requests to /messages endpoint"
    - "Traffic generator sends HTTP POST requests to /flags endpoint"
    - "Sustained rate is approximately 15-20 RPS"
    - "Messages sourced from combined_dataset.csv and synthetic HF generation"
    - "Generator script is runnable standalone via `python src/data/synthetic_traffic_generator.py`"
  artifacts:
    - path: "src/data/synthetic_traffic_generator.py"
      provides: "Async HTTP traffic generator sending messages to FastAPI"
      min_lines: 80
    - path: "tests/test_traffic_generator.py"
      provides: "Tests verifying traffic generator sends requests at target RPS"
      min_lines: 40
  key_links:
    - from: "src/data/synthetic_traffic_generator.py"
      to: "http://localhost:8000/messages"
      via: "aiohttp POST requests"
      pattern: "session\.post.*messages"
    - from: "src/data/synthetic_traffic_generator.py"
      to: "combined_dataset.csv"
      via: "csv.DictReader"
      pattern: "csv\.DictReader"
---

<objective>
Create a synthetic HTTP traffic generator that sends sustained ~15-20 RPS traffic to FastAPI endpoints.

Purpose: Simulate realistic Zulip chat traffic for testing and demo video recording. Traffic generator hits POST /messages and POST /flags endpoints with mixed data (real CSV rows + synthetic HF messages).

Output: `src/data/synthetic_traffic_generator.py` and `tests/test_traffic_generator.py`
</objective>

<execution_context>
@/home/kukiku/Desktop/NYU courses/Semester 2/MLOps/Ml_Project/.opencode/get-shit-done/workflows/execute-plan.md
@/home/kukiku/Desktop/NYU courses/Semester 2/MLOps/Ml_Project/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-real-time-processing/02-CONTEXT.md
@.planning/phases/02-real-time-processing/02-RESEARCH.md

<interfaces>
From src/api/models.py (existing — traffic generator sends payloads matching these):
```python
class MessagePayload(BaseModel):
    text: str = Field(..., min_length=1, max_length=10000)
    user_id: str = Field(...)
    source: str = Field(default="real")

class FlagPayload(BaseModel):
    message_id: str = Field(...)
    flagged_by: str | None = Field(default=None)
    reason: str | None = Field(default=None)
```

From src/data/prompts.py (existing — reused for synthetic message generation):
```python
LABEL_DISTRIBUTION = {"toxic": 0.30, "suicide": 0.30, "benign": 0.40}
PROMPTS_BY_LABEL: dict[LabelType, GenerationPrompt]
```

From src/data/synthetic_generator.py (existing — HF API call pattern to reuse):
```python
def _call_hf_api(client: InferenceClient, prompt: GenerationPrompt, max_retries: int = 3) -> Optional[str]
```

From src/utils/config.py (existing):
```python
Config.DATABASE_URL, Config.MINIO_ENDPOINT, Config.HF_TOKEN
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install aiohttp and create traffic generator</name>
  <files>src/data/synthetic_traffic_generator.py</files>
  <read_first>
    - src/data/synthetic_generator.py (HF API call pattern to reuse)
    - src/data/prompts.py (GenerationPrompt and label distribution)
    - src/api/models.py (MessagePayload, FlagPayload schemas for correct payloads)
  </read_first>
  <action>
    Create `src/data/synthetic_traffic_generator.py` — an async HTTP traffic generator per D-01 through D-04.

    Requirements:
    - D-01: Sustained stream dispatch at ~15-20 RPS
    - D-02: Async concurrency model using `asyncio` + `aiohttp`
    - D-03: Mixed data source — reads from `combined_dataset.csv` rows AND generates synthetic messages via HF API calls
    - D-04: Script location: `src/data/synthetic_traffic_generator.py`

    Implementation:
    1. Install aiohttp: `pip install aiohttp`
    2. Create the module with:
       - `TARGET_RPS = 15` constant
       - `async def load_csv_messages(filepath: str) -> list[str]` — reads combined_dataset.csv, extracts `text` column
       - `async def generate_synthetic_message() -> dict` — calls HF API via `_call_hf_api` from `src.data.synthetic_generator`, returns payload dict with text, user_id, source
       - `async def send_message(session: aiohttp.ClientSession, url: str, payload: dict) -> dict` — POST to URL, return response JSON
       - `async def run_traffic_generator(base_url: str = "http://localhost:8000", duration_seconds: int = 60, rps: int = 15)` — main loop that dispatches at target RPS
       - `if __name__ == "__main__"` block: argparse with `--base-url`, `--duration`, `--rps` flags, calls `asyncio.run(run_traffic_generator(...))`

    The generator should:
    - Load CSV messages once at startup (into memory)
    - Alternate between CSV rows (random choice) and synthetic HF messages (80/20 split)
    - POST 80% to `/messages` with {text, user_id, source} payload
    - POST 20% to `/flags` with {message_id, reason, flagged_by} payload (use random UUID for message_id)
    - Use `asyncio.sleep(1.0 / rps)` between dispatches
    - Log dispatch count every 100 requests
    - Handle connection errors gracefully (log and continue)
    - Use `logging.getLogger(__name__)` (no print statements per conventions)

    Import `_call_hf_api` from `src.data.synthetic_generator` for HF API calls.
    Import `PROMPTS_BY_LABEL` and `LABEL_DISTRIBUTION` from `src.data.prompts`.
    Import `Config` from `src.utils.config` for HF_TOKEN.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/data/synthetic_traffic_generator.py').read()); print('SYNTAX OK')"</automated>
  </verify>
  <done>
    File exists at `src/data/synthetic_traffic_generator.py` with valid Python syntax.
    Contains `run_traffic_generator` async function accepting base_url, duration_seconds, rps params.
    Contains `if __name__ == "__main__"` block with argparse.
    Imports use `aiohttp` and `asyncio`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write traffic generator tests</name>
  <files>tests/test_traffic_generator.py</files>
  <read_first>
    - src/data/synthetic_traffic_generator.py (functions to test)
    - tests/conftest.py (existing test fixtures)
  </read_first>
  <action>
    Create `tests/test_traffic_generator.py` with tests for the traffic generator.

    Tests:
    1. `test_load_csv_messages` — verifies CSV loading returns list of strings from text column
       - Use a temporary CSV file with known content
       - Assert returned list length matches row count
       - Assert each element is a non-empty string

    2. `test_send_message_makes_post_request` — verifies send_message sends POST with correct payload
       - Use aiohttp test server or mock session
       - Assert request method is POST
       - Assert JSON body contains text, user_id, source keys

    3. `test_run_traffic_generator_respects_rps` — verifies rate limiting
       - Mock the HTTP session to track dispatch times
       - Run for 2 seconds at 5 RPS
       - Assert total requests approximately 10 (±30%)
       - Assert requests are spaced by approximately 0.2s intervals

    Use `pytest.mark.asyncio` for async tests.
    Use `unittest.mock.AsyncMock` for mocking aiohttp session.
  </action>
  <verify>
    <automated>pytest tests/test_traffic_generator.py -x -v --co 2>/dev/null | head -20</automated>
  </verify>
  <done>
    `tests/test_traffic_generator.py` exists with 3+ test functions.
    All tests pass `pytest tests/test_traffic_generator.py -x -v`.
    Tests cover CSV loading, POST dispatch, and RPS rate control.
  </done>
</task>

</tasks>

<verification>
- Run `pytest tests/test_traffic_generator.py -x -v` — all tests pass
- Verify `src/data/synthetic_traffic_generator.py` has valid Python syntax
- Verify file contains `TARGET_RPS = 15` constant (D-01)
- Verify file imports `aiohttp` and `asyncio` (D-02)
- Verify file loads from `combined_dataset.csv` (D-03)
</verification>

<success_criteria>
- `src/data/synthetic_traffic_generator.py` exists with async traffic generator
- `tests/test_traffic_generator.py` exists with passing tests
- Generator sends POST /messages and POST /flags at ~15-20 RPS
- Messages sourced from CSV + synthetic HF generation
- Runnable via `python src/data/synthetic_traffic_generator.py --help`
</success_criteria>

<output>
After completion, create `.planning/phases/02-real-time-processing/02-01-SUMMARY.md`
</output>
